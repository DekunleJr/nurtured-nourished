# Development-only dependencies: not needed to run the API, only to test it.
# Install with:  .\venv\Scripts\python.exe -m pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.4.2
# httpx backs FastAPI's TestClient (tests/test_admin_routing.py etc.).
httpx==0.28.1
